import React, { useState, useEffect } from 'react';
import { Table } from 'antd';
import { get_set } from 'discrete-core';

function TableList() {
  const aa = get_set('a');

  return (
    <div>23</div>
  );
}

export default (TableList);

export async function getStaticProps() {
}
